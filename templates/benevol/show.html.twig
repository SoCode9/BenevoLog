{% extends 'base.html.twig' %}

{% block title %}Benevol{% endblock %}

{% block body %}
    <h1>Benevol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ benevol.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ benevol.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ benevol.lastName }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ benevol.gender }}</td>
            </tr>
            <tr>
                <th>Birth_date</th>
                <td>{{ benevol.birthDate ? benevol.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ benevol.email }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ benevol.address }}</td>
            </tr>
            <tr>
                <th>Private_phone</th>
                <td>{{ benevol.privatePhone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_benevol_index') }}">back to list</a>

    <a href="{{ path('app_benevol_edit', {'id': benevol.id}) }}">edit</a>

    {{ include('benevol/_delete_form.html.twig') }}
{% endblock %}
